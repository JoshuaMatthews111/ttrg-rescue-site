"use client";

import { useState, useEffect, useRef } from "react";
import Link from "next/link";
import {
  Heart, PawPrint, Users, ChevronRight, ArrowRight, Play, X,
  Handshake, Home, Gift, DollarSign, UserPlus, HeartHandshake,
  Stethoscope, GraduationCap, Smile, Shield, Eye, Award,
  AlertTriangle, Clock, CheckCircle2, Sparkles,
} from "lucide-react";
import { dogs as dogData, donationTiers } from "@/lib/dogs";
import { getPublishedDogs } from "@/lib/admin-store";

/* ─── DATA ─── */
const stats = [
  { icon: Shield, value: 2300, suffix: "+", label: "Dogs Rescued", desc: "From high-risk situations" },
  { icon: Heart, value: 1850, suffix: "+", label: "Dogs Rehabilitated", desc: "Healing with care & love" },
  { icon: Home, value: 1600, suffix: "+", label: "Dogs Rehomed", desc: "Forever families found" },
  { icon: Users, value: 2000, suffix: "+", label: "Supporters", desc: "Making it all possible" },
];

const testimonialVideos = [
  { id: 1, src: "/ttrg/videos/britta-testimonial.mp4", title: "From Fear to Family", quote: "Luna's journey from heartbreak to thriving.", category: "Rescue Story" },
  { id: 2, src: "/ttrg/videos/testimonial-2.mp4", title: "Second Chances Work", quote: "Max went from neglected to thriving.", category: "Training Story" },
  { id: 3, src: "/ttrg/videos/trefz-family.mp4", title: "A Bond That Heals", quote: "How Rex helped a family heal too.", category: "Client Testimonial" },
];


const journey = [
  { num: 1, label: "Rescue", icon: Shield, desc: "We save dogs from high-risk situations.", color: "#C41E2A" },
  { num: 2, label: "Rehabilitate", icon: Stethoscope, desc: "We provide medical care and healing.", color: "#C41E2A" },
  { num: 3, label: "Train", icon: GraduationCap, desc: "We build confidence, skills, and trust.", color: "#C41E2A" },
  { num: 4, label: "Recover", icon: Smile, desc: "We restore their health and happiness.", color: "#C41E2A" },
  { num: 5, label: "Rehome", icon: Home, desc: "We match them with loving forever families.", color: "#C41E2A" },
];

const stageIndex: Record<string, number> = { rescue: 0, rehabilitate: 1, train: 2, recover: 3, rehome: 4 };
const stageLabels = ["Rescue", "Rehabilitate", "Train", "Recover", "Rehome"];
const urgentLabels: Record<string, string> = { rescue: "Emergency Rescue", rehabilitate: "Needs Medical Care", train: "Waiting for Sponsor", recover: "Recovering", rehome: "Ready for Adoption" };
const daysInRescue: Record<string, number> = { bailey: 47, tucker: 23, daisy: 31, shadow: 62, prince: 12, luna: 38 };

const trainers = [
  { name: "Lorenzo Miller", role: "Lead Trainer", specialty: "Behavior & Rehabilitation", exp: "12+", photo: "/ttrg/trainers/lorenzo-miller-candid.jpg" },
  { name: "Jasmine Bland", role: "Adoption & Foster", specialty: "Solutions & Enrichment", exp: "8+", photo: "/ttrg/trainers/jasmine-bland-candid.jpg" },
  { name: "Daniel Bainbridge", role: "K9 Specialist", specialty: "Training & Behavior", exp: "10+", photo: "/ttrg/trainers/daniel-bainbridge-rd-candid.jpg" },
  { name: "Bailey Brown", role: "Puppy Trainer", specialty: "Socialization & Basics", exp: "6+", photo: "/ttrg/trainers/bailey-brown-candid.jpg" },
];

const getInvolved = [
  { icon: HeartHandshake, label: "Sponsor", desc: "Provide monthly support", href: "/ttrg/sponsor" },
  { icon: Home, label: "Foster", desc: "Open your home temporarily", href: "/ttrg/foster" },
  { icon: Handshake, label: "Volunteer", desc: "Share your time and skills", href: "/ttrg/volunteer" },
  { icon: DollarSign, label: "Donate", desc: "Give once or monthly", href: "/ttrg/donate" },
  { icon: Gift, label: "Partner", desc: "Support as a business", href: "/ttrg/contact" },
  { icon: UserPlus, label: "Monthly Supporter", desc: "Be their hero every month", href: "/ttrg/donate" },
];

/* ─── SCROLL ANIMATION HOOK ─── */
function useInView(threshold = 0.15) {
  const ref = useRef<HTMLDivElement>(null);
  const [visible, setVisible] = useState(false);
  useEffect(() => {
    const el = ref.current;
    if (!el) return;
    const obs = new IntersectionObserver(([e]) => { if (e.isIntersecting) { setVisible(true); obs.disconnect(); } }, { threshold });
    obs.observe(el);
    return () => obs.disconnect();
  }, [threshold]);
  return { ref, visible };
}

/* ─── ANIMATED COUNTER ─── */
function Counter({ target, suffix }: { target: number; suffix: string }) {
  const [count, setCount] = useState(0);
  const ref = useRef<HTMLSpanElement>(null);
  const started = useRef(false);

  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting && !started.current) {
          started.current = true;
          const duration = 2000;
          const step = Math.max(1, Math.floor(target / (duration / 16)));
          const interval = setInterval(() => {
            setCount((prev) => {
              if (prev + step >= target) { clearInterval(interval); return target; }
              return prev + step;
            });
          }, 16);
        }
      },
      { threshold: 0.3 }
    );
    if (ref.current) observer.observe(ref.current);
    return () => observer.disconnect();
  }, [target]);

  return <span ref={ref}>{count.toLocaleString()}{suffix}</span>;
}

/* ─── SMOOTH SCROLL HELPER ─── */
function scrollToId(id: string) {
  const el = document.getElementById(id);
  if (el) el.scrollIntoView({ behavior: "smooth", block: "start" });
}

/* ─── PAGE ─── */
export default function TTRGHome() {
  const [videoModal, setVideoModal] = useState<null | typeof testimonialVideos[0]>(null);
  const [popupOpen, setPopupOpen] = useState(false);
  const [popupDismissed, setPopupDismissed] = useState(false);
  const [popupEmail, setPopupEmail] = useState("");
  const [popupSubmitted, setPopupSubmitted] = useState(false);
  const [dogs, setDogs] = useState(dogData);

  useEffect(() => {
    const published = getPublishedDogs();
    if (published.length > 0) setDogs(published as unknown as typeof dogData);
  }, []);

  // Popup trigger: after 6s or 35% scroll
  useEffect(() => {
    if (typeof window === "undefined") return;
    if (sessionStorage.getItem("ttrg-popup-closed")) return;
    const timer = setTimeout(() => { if (!popupDismissed) setPopupOpen(true); }, 6000);
    const onScroll = () => {
      const pct = window.scrollY / (document.body.scrollHeight - window.innerHeight);
      if (pct > 0.35 && !popupDismissed) { setPopupOpen(true); window.removeEventListener("scroll", onScroll); }
    };
    window.addEventListener("scroll", onScroll);
    return () => { clearTimeout(timer); window.removeEventListener("scroll", onScroll); };
  }, [popupDismissed]);

  const closePopup = () => { setPopupOpen(false); setPopupDismissed(true); sessionStorage.setItem("ttrg-popup-closed", "1"); };

  const hero = useInView();
  const journeySection = useInView();
  const dogsSection = useInView();
  const videoSection = useInView();
  const impactSection = useInView();

  return (
    <div className="bg-white">

      {/* ═══ 1. HERO — CINEMATIC ═══ */}
      <section ref={hero.ref} className="relative min-h-[85vh] sm:min-h-[92vh] flex items-center overflow-hidden">
        <div className="absolute inset-0 animate-slow-zoom">
          <img src="/ttrg/hero-trainer.png" alt="TTRG trainer with rescue dog" className="w-full h-full object-cover object-right" />
        </div>
        <div className="absolute inset-0 bg-gradient-to-r from-[#0a1628]/95 via-[#0a1628]/75 to-[#0a1628]/20 md:from-[#0a1628]/90 md:via-[#0a1628]/55 md:to-transparent" />

        <div className={`relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 w-full ${hero.visible ? "animate-fade-up" : "opacity-0"}`}>
          <div className="max-w-2xl py-8 sm:py-12">
            <div className="inline-flex items-center gap-2 bg-white/10 backdrop-blur-sm border border-white/20 rounded-full px-4 py-1.5 mb-6">
              <span className="w-2 h-2 rounded-full bg-[#C41E2A] animate-pulse" />
              <span className="text-white/70 text-xs font-medium">3 dogs rescued this week</span>
            </div>

            <h1 className="text-4xl sm:text-6xl md:text-7xl lg:text-8xl font-black text-white leading-[0.95] mb-3 tracking-tight">
              Rescue.<br />Rehab.<br /><span className="text-[#C41E2A]">Rehome.</span>
            </h1>
            <p className="text-xl sm:text-2xl font-light text-white/50 mb-2">
              Repeat. <Heart className="inline w-5 h-5 text-[#C41E2A] fill-[#C41E2A] align-middle" />
            </p>

            <p className="text-white/60 text-base sm:text-lg leading-relaxed max-w-lg mt-6 mb-8">
              Every rescue dog deserves safety, healing, and a second chance. We transform the forgotten into the unforgettable.
            </p>

            <div className="flex flex-col sm:flex-row gap-4">
              <button onClick={() => scrollToId("dogs-in-need")} className="inline-flex items-center justify-center gap-2 bg-[#C41E2A] text-white px-8 py-4 rounded-full text-sm font-bold transition-all btn-glow animate-pulse-glow">
                <Heart className="w-4 h-4 fill-white" /> HELP SAVE DOGS
              </button>
              <Link href="/ttrg/journeys" className="inline-flex items-center justify-center gap-2 border-2 border-white/30 text-white px-8 py-4 rounded-full text-sm font-bold hover:bg-white/10 transition-all btn-glow-secondary">
                <Play className="w-4 h-4" /> VIEW RESCUE JOURNEYS
              </Link>
            </div>

            <div className="flex items-center gap-6 mt-10 text-white/30 text-xs">
              <span className="flex items-center gap-1"><Shield className="w-3 h-3" /> 501(c)(3) Nonprofit</span>
              <span className="flex items-center gap-1"><PawPrint className="w-3 h-3" /> 2,300+ Dogs Rescued</span>
            </div>
          </div>
        </div>

        <div className="absolute bottom-10 right-10 hidden lg:block animate-float opacity-20">
          <PawPrint className="w-20 h-20 text-white" />
        </div>
      </section>

      {/* ═══ 2. EMOTIONAL QUOTE + EMAIL CAPTURE ═══ */}
      <section className="relative py-10 sm:py-12 bg-[#1B2A4A]">
        <div className="max-w-3xl mx-auto px-4 text-center">
          <p className="text-white/50 text-sm sm:text-base italic mb-6">
            &ldquo;The greatness of a nation can be judged by the way its animals are treated.&rdquo;
            <span className="text-white/30 ml-2">— Mahatma Gandhi</span>
          </p>
          <h3 className="text-white text-lg sm:text-xl font-bold mb-2">A Rescue Dog Needs Someone Like You</h3>
          <p className="text-white/40 text-sm mb-5 max-w-lg mx-auto">Know a dog that needs rescue, rehabilitation, or a second chance? Join our rescue network and help save lives.</p>
          <form onSubmit={(e) => { e.preventDefault(); }} className="flex flex-col sm:flex-row gap-3 max-w-md mx-auto">
            <input type="email" placeholder="Enter your email" className="flex-1 h-12 px-4 rounded-xl bg-white/10 border border-white/20 text-white text-sm placeholder:text-white/30 focus:outline-none focus:ring-2 focus:ring-[#C41E2A]/50" />
            <button type="submit" className="bg-[#C41E2A] hover:bg-[#A01825] text-white px-6 h-12 rounded-xl text-sm font-bold transition-colors whitespace-nowrap flex items-center justify-center gap-2">
              <Heart className="w-4 h-4 fill-white" /> JOIN THE MISSION
            </button>
          </form>
          <p className="text-white/20 text-[10px] mt-3">No spam. Just rescue updates and ways to help.</p>
        </div>
      </section>

      {/* ═══ 3. IMPACT STATS — ANIMATED ═══ */}
      <section ref={impactSection.ref} className="py-16 bg-[#FAFAF8] border-b border-slate-100">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="grid grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6">
            {stats.map((stat, idx) => (
              <div
                key={stat.label}
                className={`text-center p-5 sm:p-7 rounded-2xl bg-white border border-slate-100 hover:shadow-lg hover:-translate-y-1 transition-all duration-500 ${impactSection.visible ? "animate-fade-up" : "opacity-0"}`}
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="w-12 h-12 rounded-2xl bg-[#FFF0F0] flex items-center justify-center mx-auto mb-3">
                  <stat.icon className="w-6 h-6 text-[#C41E2A]" />
                </div>
                <div className="text-2xl sm:text-4xl font-black text-[#1B2A4A] mb-1">
                  <Counter target={stat.value} suffix={stat.suffix} />
                </div>
                <p className="text-xs sm:text-sm font-bold text-[#1B2A4A]">{stat.label}</p>
                <p className="text-[10px] sm:text-[11px] text-[#1B2A4A]/40 mt-0.5">{stat.desc}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* ═══ 4. OUR DOG JOURNEY — 5-STEP PROCESS ═══ */}
      <section ref={journeySection.ref} className="py-20 sm:py-24">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className={`text-center mb-12 ${journeySection.visible ? "animate-fade-up" : "opacity-0"}`}>
            <div className="inline-flex items-center gap-2 bg-[#C41E2A]/10 rounded-full px-4 py-1.5 mb-4">
              <PawPrint className="w-3.5 h-3.5 text-[#C41E2A]" />
              <span className="text-[#C41E2A] text-xs font-bold uppercase tracking-wider">Our Process</span>
            </div>
            <h2 className="text-2xl sm:text-3xl md:text-4xl font-black text-[#1B2A4A]">Our Dog Journey</h2>
            <p className="text-sm text-[#1B2A4A]/50 mt-2 max-w-xl mx-auto">A proven path to healing, growth, and home. Every dog follows these steps.</p>
          </div>

          {/* Journey Steps — with connecting lines */}
          <div className="relative">
            <div className="hidden md:block absolute top-10 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-[#C41E2A]/20 via-[#C41E2A]/40 to-[#C41E2A]/20" />

            <div className="flex flex-col md:flex-row items-stretch md:items-start justify-between gap-8 md:gap-4">
              {journey.map((step, i) => (
                <div
                  key={step.label}
                  className={`flex items-center gap-4 md:flex-col md:gap-3 md:text-center flex-1 ${journeySection.visible ? "animate-fade-up" : "opacity-0"}`}
                  style={{ animationDelay: `${i * 0.12}s` }}
                >
                  <div className="relative z-10">
                    <div className="w-20 h-20 rounded-full bg-white border-2 border-[#C41E2A]/20 flex items-center justify-center shadow-md hover:shadow-lg hover:scale-105 transition-all">
                      <step.icon className="w-8 h-8 text-[#C41E2A]" />
                    </div>
                    <span className="absolute -top-1 -right-1 w-7 h-7 rounded-full bg-[#C41E2A] text-white text-[11px] font-bold flex items-center justify-center shadow-sm">
                      {step.num}
                    </span>
                  </div>
                  <div className="md:mt-1">
                    <p className="font-bold text-[#1B2A4A] text-base">{step.label}</p>
                    <p className="text-xs text-[#1B2A4A]/50 mt-1 max-w-[200px]">{step.desc}</p>
                  </div>
                </div>
              ))}
            </div>
          </div>

          <div className="text-center mt-12">
            <Link href="/ttrg/journeys" className="inline-flex items-center gap-2 bg-[#1B2A4A] hover:bg-[#2a3d66] text-white px-7 py-3.5 rounded-full text-sm font-bold transition-all btn-glow-secondary">
              <PawPrint className="w-4 h-4" /> SEE FULL DOG JOURNEYS
            </Link>
          </div>
        </div>
      </section>

      {/* ═══ 5. DOGS WHO NEED SUPPORT ═══ */}
      <section id="dogs-in-need" ref={dogsSection.ref} className="py-20 sm:py-24 bg-[#FAFAF8]">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className={`text-center mb-12 ${dogsSection.visible ? "animate-fade-up" : "opacity-0"}`}>
            <div className="inline-flex items-center gap-2 bg-[#C41E2A]/10 rounded-full px-4 py-1.5 mb-4">
              <Heart className="w-3.5 h-3.5 text-[#C41E2A]" />
              <span className="text-[#C41E2A] text-xs font-bold uppercase tracking-wider">Dogs In Need</span>
            </div>
            <h2 className="text-2xl sm:text-3xl md:text-4xl font-black text-[#1B2A4A]">Dogs Who Need Your Support</h2>
            <p className="text-sm text-[#1B2A4A]/50 mt-2 max-w-xl mx-auto">Your monthly gift provides care, training, and a second chance at life. Choose how you want to help.</p>
          </div>

          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
            {dogs.map((dog, idx) => {
              const si = stageIndex[dog.stage] ?? 0;
              const days = daysInRescue[dog.id] ?? 0;
              return (
                <div
                  key={dog.id}
                  className={`bg-white rounded-3xl overflow-hidden border border-slate-100 hover:shadow-2xl hover:-translate-y-1 transition-all duration-500 group ${dogsSection.visible ? "animate-fade-up" : "opacity-0"}`}
                  style={{ animationDelay: `${idx * 0.1}s` }}
                >
                  <Link href={`/ttrg/dogs/${dog.id}`} className="block">
                    <div className="relative h-48 sm:h-56 overflow-hidden">
                      <img src={dog.image} alt={dog.name} className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700" />
                      <div className="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent" />

                      {dog.urgent && (
                        <div className="absolute top-3 left-3 flex items-center gap-1.5 bg-red-600 text-white px-3 py-1 rounded-full text-[10px] font-bold animate-pulse">
                          <AlertTriangle className="w-3 h-3" /> URGENT
                        </div>
                      )}

                      <div className="absolute top-3 right-3 bg-black/40 backdrop-blur-sm text-white px-3 py-1 rounded-full text-[10px] font-semibold">
                        {urgentLabels[dog.stage]}
                      </div>

                      <div className="absolute bottom-3 left-3 flex items-center gap-1.5 text-white/80 text-[10px]">
                        <Clock className="w-3 h-3" /> {days} days in rescue
                      </div>

                      <div className="absolute bottom-3 right-3">
                        <p className="text-white font-bold text-lg">{dog.name}</p>
                      </div>
                    </div>
                  </Link>

                  <div className="p-5">
                    <p className="text-[11px] text-[#1B2A4A]/40 mb-2">{dog.age} · {dog.breed} · {dog.gender}</p>
                    <p className="text-sm text-[#1B2A4A]/60 leading-relaxed mb-4 line-clamp-2">{dog.story}</p>

                    {/* Suggested Donations */}
                    <div className="mb-4">
                      <p className="text-[10px] text-[#1B2A4A]/40 font-semibold uppercase tracking-wider mb-2">Your Donation Can Provide</p>
                      <div className="grid grid-cols-2 gap-1.5">
                        {donationTiers.slice(0, 4).map((tier) => (
                          <Link
                            key={tier.amount}
                            href={`/ttrg/dogs/${dog.id}`}
                            className="text-center px-2 py-2 rounded-lg border border-slate-100 hover:border-[#C41E2A] hover:bg-[#FFF0F0] transition-all group/tier"
                          >
                            <p className="text-sm font-bold text-[#1B2A4A] group-hover/tier:text-[#C41E2A]">${tier.amount}<span className="text-[9px] font-normal text-[#1B2A4A]/40">/mo</span></p>
                            <p className="text-[9px] text-[#1B2A4A]/40 leading-tight mt-0.5">{tier.label}</p>
                          </Link>
                        ))}
                      </div>
                    </div>

                    {/* CTAs */}
                    <div className="flex gap-2">
                      <Link href={`/ttrg/dogs/${dog.id}`} className="flex-1 bg-[#C41E2A] hover:bg-[#A01825] text-white text-center py-3 rounded-xl text-xs font-bold transition-all btn-glow flex items-center justify-center gap-1.5">
                        <Heart className="w-3.5 h-3.5 fill-white" /> SPONSOR {dog.name.toUpperCase()}
                      </Link>
                      <Link href={`/ttrg/dogs/${dog.id}`} className="flex items-center justify-center w-11 border border-slate-200 rounded-xl hover:bg-slate-50 transition-colors">
                        <Eye className="w-4 h-4 text-[#1B2A4A]/40" />
                      </Link>
                    </div>
                    <p className="text-[9px] text-center text-[#1B2A4A]/30 mt-2">View profile for custom amount & recurring options</p>
                  </div>
                </div>
              );
            })}
          </div>

          <div className="text-center mt-10">
            <Link href="/ttrg/sponsor" className="inline-flex items-center gap-2 border-2 border-[#C41E2A] text-[#C41E2A] px-7 py-3.5 rounded-full text-sm font-bold hover:bg-[#C41E2A] hover:text-white transition-all">
              VIEW ALL DOGS <ChevronRight className="w-4 h-4" />
            </Link>
          </div>
        </div>
      </section>

      {/* ═══ 6. TESTIMONIAL VIDEOS — CINEMATIC ═══ */}
      <section ref={videoSection.ref} className="py-20 sm:py-24">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className={`text-center mb-12 ${videoSection.visible ? "animate-fade-up" : "opacity-0"}`}>
            <div className="inline-flex items-center gap-2 bg-[#1B2A4A]/10 rounded-full px-4 py-1.5 mb-4">
              <Play className="w-3.5 h-3.5 text-[#1B2A4A]" />
              <span className="text-[#1B2A4A] text-xs font-bold uppercase tracking-wider">Video Testimonials</span>
            </div>
            <h2 className="text-2xl sm:text-3xl md:text-4xl font-black text-[#1B2A4A]">Real Clients. Real Dogs. Real Transformation.</h2>
            <p className="text-sm text-[#1B2A4A]/50 mt-2 max-w-xl mx-auto">See how rescue, training, and committed support can change the life of a dog and the family connected to them.</p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            {testimonialVideos.map((vid, idx) => (
              <button
                key={vid.id}
                onClick={() => setVideoModal(vid)}
                className={`group text-left rounded-3xl overflow-hidden border border-slate-100 hover:shadow-2xl transition-all duration-500 ${videoSection.visible ? "animate-fade-up" : "opacity-0"}`}
                style={{ animationDelay: `${idx * 0.15}s` }}
              >
                <div className="relative aspect-[16/10] overflow-hidden bg-slate-900">
                  <video muted loop playsInline preload="metadata" className="w-full h-full object-cover brightness-75 group-hover:brightness-50 group-hover:scale-105 transition-all duration-700" onMouseOver={(e) => (e.target as HTMLVideoElement).play()} onMouseOut={(e) => { const v = e.target as HTMLVideoElement; v.pause(); v.currentTime = 0; }}>
                    <source src={vid.src} type="video/mp4" />
                  </video>
                  <div className="absolute inset-0 bg-gradient-to-t from-black/70 via-transparent to-transparent" />
                  <div className="absolute inset-0 flex items-center justify-center pointer-events-none">
                    <div className="w-16 h-16 rounded-full bg-white/20 backdrop-blur-sm flex items-center justify-center group-hover:bg-[#C41E2A] group-hover:scale-110 transition-all duration-300 shadow-lg">
                      <Play className="w-6 h-6 text-white fill-white ml-1" />
                    </div>
                  </div>
                  <div className="absolute bottom-0 left-0 right-0 p-5">
                    <span className="inline-block bg-[#C41E2A]/90 text-white text-[9px] font-bold uppercase tracking-wider px-2 py-0.5 rounded-full mb-2">{vid.category}</span>
                    <p className="text-white font-bold text-sm">{vid.title}</p>
                    <p className="text-white/60 text-xs italic mt-1">&ldquo;{vid.quote}&rdquo;</p>
                  </div>
                </div>
              </button>
            ))}
          </div>

          <div className="text-center mt-8">
            <Link href="/ttrg/stories" className="inline-flex items-center gap-2 text-sm font-bold text-[#1B2A4A] hover:text-[#C41E2A] transition-colors">
              VIEW ALL VIDEOS <ChevronRight className="w-4 h-4" />
            </Link>
          </div>
        </div>
      </section>

      {/* ═══ 7. MEET OUR TRAINERS ═══ */}
      <section className="py-20 bg-[#FAFAF8]">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-12">
            <h2 className="text-2xl sm:text-3xl font-bold text-[#1B2A4A]">Meet Our Trainers</h2>
            <p className="text-sm text-[#1B2A4A]/50 mt-1">Experienced. Compassionate. Committed.</p>
          </div>

          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {trainers.map((t) => (
              <Link key={t.name} href="/ttrg/trainers" className="bg-white rounded-2xl overflow-hidden border border-slate-100 hover:shadow-lg transition-all block">
                <div className="h-56 overflow-hidden">
                  <img src={t.photo} alt={t.name} className="w-full h-full object-cover object-top hover:scale-105 transition-transform duration-500" />
                </div>
                <div className="p-5">
                  <h3 className="font-bold text-[#1B2A4A] text-base">{t.name}</h3>
                  <p className="text-[#C41E2A] text-xs font-semibold">{t.role}</p>
                  <p className="text-[11px] text-[#1B2A4A]/40 mt-1">{t.specialty}</p>
                  <div className="flex items-center gap-3 mt-3 text-[11px] text-[#1B2A4A]/50">
                    <span className="flex items-center gap-1"><Award className="w-3 h-3" /> {t.exp} years experience</span>
                  </div>
                </div>
              </Link>
            ))}
          </div>
        </div>
      </section>

      {/* ═══ 8. GET INVOLVED ═══ */}
      <section className="py-20">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-12">
            <h2 className="text-2xl sm:text-3xl font-bold text-[#1B2A4A]">Get Involved</h2>
            <p className="text-sm text-[#1B2A4A]/50 mt-1">Many ways to make a lasting impact.</p>
          </div>

          <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
            {getInvolved.map((item) => (
              <Link
                key={item.label}
                href={item.href}
                className="group bg-[#FAFAF8] hover:bg-white rounded-2xl border border-slate-100 p-5 text-center hover:shadow-lg transition-all"
              >
                <div className="w-12 h-12 rounded-2xl bg-white group-hover:bg-[#FFF0F0] flex items-center justify-center mx-auto mb-3 transition-colors shadow-sm">
                  <item.icon className="w-5 h-5 text-[#C41E2A]" />
                </div>
                <p className="font-bold text-[#1B2A4A] text-sm">{item.label}</p>
                <p className="text-[10px] text-[#1B2A4A]/40 mt-1">{item.desc}</p>
                <ArrowRight className="w-4 h-4 text-[#C41E2A] mx-auto mt-2 opacity-0 group-hover:opacity-100 transition-opacity" />
              </Link>
            ))}
          </div>
        </div>
      </section>

      {/* ═══ 9. SUBMIT A DOG ═══ */}
      <section className="py-16 bg-[#1B2A4A]">
        <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <PawPrint className="w-10 h-10 text-[#C41E2A] mx-auto mb-4" />
          <h2 className="text-2xl sm:text-3xl font-bold text-white mb-3">Know a Dog That Needs Help?</h2>
          <p className="text-white/50 text-sm max-w-lg mx-auto mb-6">If you&apos;re a shelter, rescue partner, trainer, or individual with a dog that needs rescue, rehabilitation, or rehoming — we want to hear from you.</p>
          <Link href="/ttrg/submit" className="inline-flex items-center gap-2 bg-[#C41E2A] hover:bg-[#A01825] text-white px-8 py-4 rounded-full text-sm font-bold transition-colors btn-glow">
            SUBMIT A DOG FOR RESCUE <ArrowRight className="w-4 h-4" />
          </Link>
        </div>
      </section>

      {/* ═══ 10. FEATURED STORY: PRINCE ═══ */}
      <section className="py-20 bg-[#FAFAF8]">
        <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="bg-white rounded-2xl overflow-hidden border border-slate-100 shadow-lg">
            <div className="grid grid-cols-1 md:grid-cols-2">
              <div className="relative h-72 md:h-auto">
                <img src="/ttrg/testimonial-photo.jpg" alt="Meet Prince" className="w-full h-full object-cover" />
              </div>
              <div className="p-8 md:p-10 flex flex-col justify-center">
                <span className="text-[#C41E2A] text-xs font-bold uppercase tracking-wider mb-2">Featured Rescue Story</span>
                <h3 className="text-2xl sm:text-3xl font-bold text-[#1B2A4A] mb-3">Meet Prince</h3>
                <p className="text-[#1B2A4A]/60 text-sm leading-relaxed mb-4">
                  A 1 year old, Doberman mix who had behavioral challenges such as jumping, pulling, and running away. Prince was in serious need of training, to keep him and others safe.
                </p>
                <Link href="/ttrg/dogs/prince" className="inline-flex items-center gap-2 text-[#C41E2A] text-sm font-bold hover:underline">
                  See Prince&apos;s full profile <ArrowRight className="w-4 h-4" />
                </Link>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* ═══ 10. FINAL CTA ═══ */}
      <section className="relative py-20 sm:py-24 overflow-hidden">
        <div className="absolute inset-0 bg-[#1B2A4A]" />
        <div className="relative z-10 max-w-3xl mx-auto px-4 text-center">
          <Heart className="w-12 h-12 text-[#C41E2A] fill-[#C41E2A]/30 mx-auto mb-6 animate-float" />
          <h2 className="text-2xl sm:text-3xl md:text-4xl font-black text-white leading-tight mb-6">
            Be the Reason a Dog Gets a Second Chance
          </h2>
          <p className="text-white/50 text-sm sm:text-base max-w-xl mx-auto mb-10">
            Sponsor a dog&apos;s recovery, report a dog in need, or join our rescue network. Every action saves a life.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <button onClick={() => scrollToId("dogs-in-need")} className="inline-flex items-center justify-center gap-2 bg-[#C41E2A] text-white px-8 py-4 rounded-full text-sm font-bold transition-all btn-glow animate-pulse-glow">
              <Heart className="w-4 h-4 fill-white" /> SPONSOR A DOG
            </button>
            <Link href="/ttrg/contact" className="inline-flex items-center justify-center gap-2 border-2 border-white/30 text-white px-8 py-4 rounded-full text-sm font-bold hover:bg-white/10 transition-all">
              <PawPrint className="w-4 h-4" /> REPORT A DOG IN NEED
            </Link>
          </div>
        </div>
      </section>

      {/* ═══ VIDEO LIGHTBOX ═══ */}
      {videoModal && (
        <div className="fixed inset-0 z-[100] bg-black/90 flex items-center justify-center p-4" onClick={() => setVideoModal(null)}>
          <div className="relative w-full max-w-4xl" onClick={(e) => e.stopPropagation()}>
            <button onClick={() => setVideoModal(null)} className="absolute -top-12 right-0 text-white/60 hover:text-white transition-colors">
              <X className="w-8 h-8" />
            </button>
            <div className="rounded-2xl overflow-hidden shadow-2xl bg-black">
              <video controls autoPlay className="w-full aspect-video">
                <source src={videoModal.src} type="video/mp4" />
              </video>
              <div className="p-5 bg-[#1B2A4A]">
                <p className="text-white font-bold text-lg">{videoModal.title}</p>
                <p className="text-white/50 text-sm mt-1">{videoModal.quote}</p>
              </div>
            </div>
          </div>
        </div>
      )}

      {/* ═══ RESCUE POPUP ═══ */}
      {popupOpen && (
        <div className="fixed inset-0 z-[999] flex items-center justify-center p-4">
          <div className="absolute inset-0 bg-black/60 backdrop-blur-sm animate-fade-in" onClick={closePopup} />
          <div className="relative z-10 max-w-md w-full bg-white rounded-3xl overflow-hidden animate-scale-in shadow-2xl">
            <div className="relative h-44 sm:h-48 overflow-hidden bg-[#1B2A4A]">
              <div className="absolute inset-0 flex items-center justify-center">
                <img src="/ttrg/ttrg-logo.jpeg" alt="TTRG" className="w-20 h-20 rounded-full object-cover opacity-80" />
              </div>
              <div className="absolute inset-0 bg-gradient-to-t from-white via-transparent to-transparent opacity-40" />
              <button onClick={closePopup} className="absolute top-3 right-3 w-8 h-8 rounded-full bg-black/30 backdrop-blur-sm flex items-center justify-center text-white hover:bg-black/50 transition-colors">
                <X className="w-4 h-4" />
              </button>
            </div>

            <div className="px-6 pb-6 -mt-4 relative">
              <div className="w-12 h-12 rounded-full bg-[#C41E2A] flex items-center justify-center mx-auto mb-4 animate-pulse-glow">
                <Heart className="w-6 h-6 text-white fill-white" />
              </div>

              {!popupSubmitted ? (
                <>
                  <h3 className="text-xl font-black text-[#1B2A4A] text-center mb-2">
                    A Rescue Dog Needs<br />Someone Like You
                  </h3>
                  <p className="text-[#1B2A4A]/50 text-sm text-center mb-6 leading-relaxed">
                    Know a dog that needs rescue, rehabilitation, or a second chance? Join our rescue network and help save lives.
                  </p>
                  <form onSubmit={(e) => { e.preventDefault(); setPopupSubmitted(true); }} className="space-y-3">
                    <input
                      type="email"
                      required
                      value={popupEmail}
                      onChange={(e) => setPopupEmail(e.target.value)}
                      placeholder="Your email address"
                      className="w-full h-12 px-4 rounded-xl border border-slate-200 text-sm focus:outline-none focus:ring-2 focus:ring-[#C41E2A]/30 focus:border-[#C41E2A]"
                    />
                    <button type="submit" className="w-full bg-[#C41E2A] hover:bg-[#A01825] text-white h-12 rounded-xl text-sm font-bold transition-all btn-glow flex items-center justify-center gap-2">
                      <Heart className="w-4 h-4 fill-white" /> JOIN THE RESCUE MISSION
                    </button>
                  </form>
                  <p className="text-[10px] text-[#1B2A4A]/30 text-center mt-3">We respect your privacy. No spam, ever.</p>
                </>
              ) : (
                <div className="text-center">
                  <CheckCircle2 className="w-12 h-12 text-emerald-500 mx-auto mb-4" />
                  <h3 className="text-xl font-black text-[#1B2A4A] mb-2">Thank You for Joining<br />the Rescue Mission</h3>
                  <p className="text-[#1B2A4A]/50 text-sm mb-6 leading-relaxed">
                    Our team will keep you updated on dogs who need help, sponsorship, foster support, and rescue opportunities.
                  </p>
                  <button onClick={closePopup} className="w-full bg-[#1B2A4A] hover:bg-[#1B2A4A]/80 text-white py-3 rounded-xl text-sm font-bold transition-colors flex items-center justify-center gap-2">
                    <X className="w-4 h-4" /> CLOSE
                  </button>
                </div>
              )}
            </div>
          </div>
        </div>
      )}

    </div>
  );
}
